app = "railway-sshh-qbxzfq"
primary_region = "iad"

# =====================================================
# ⚙️ Dịch vụ chính: Web (HTTP + HTTPS)
# =====================================================
[[services]]
  internal_port = 80
  protocol = "tcp"

  [[services.ports]]
    port = 80
    handlers = ["http"]

[[services]]
  internal_port = 443
  protocol = "tcp"

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]

# =====================================================
# 🖥️ SSH Service (dành cho Termius / CLI)
# =====================================================
[[services]]
  internal_port = 22
  protocol = "tcp"

  [[services.ports]]
    port = 22

# =====================================================
# 💻 Cấu hình máy ảo
# =====================================================
[[vm]]
  cpu_kind = "shared"
  cpus = 4
  memory_mb = 4096
